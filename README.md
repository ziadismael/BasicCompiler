# Compiler Recursive Descent Parser Extension

**Course:** Compilers 
**Institution:** Faculty of Computers and Artificial Intelligence, Cairo University  

## 📌 Project Overview
This project involves modifying a C++ Recursive Descent Parser to implement a new binary operator with specific mathematical logic and precedence rules. The implementation adheres to strict coding constraints, avoiding STL libraries and following a procedural C-style approach.


### Constraints & Coding Style
* **Language:** Standard C++ without STL (`string`, `vector`, etc. are forbidden). 
* **Libraries:** Only `<cstdlib>`, `<cstdio>`, `<cstring>`, `<iostream>` are allowed. 
* **Formatting:**
    * No TAB characters allowed (must be converted to 4 spaces). 
    * Strict adherence to `CodingStyle.pdf` (e.g., stack allocation `int a[n]` instead of `new`).
* **Size:** Source code must be under 15,000 characters. 

## 📂 Repository Structure

```text
Assignment1_Compiler/
├── src/
│   └── compiler.cpp              # Main implementation (Modified Parser)
├── tools/
│   └── assign_encoder.cpp        # University-provided submission encoder
├── docs/
│   ├── Assignment1.txt           # Official Task Description
│   └── CodingStyle.pdf           # Style Guide
└── README.md
```